Задача 10 / Task 10

Составить программу для вычисления значений функции F(x) на отрезке [а, b] с шагом h.
Результат представить в виде таблицы, первый столбец которой – значения аргумента,
второй - соответствующие значения функции.
                F(x) = tg(x)

Create a program for calculating the values of the function F(x) on the segment [a, b] with a step h.
The result is presented as a table with the first column containing the argument values and the second
column containing the corresponding function values.
                F(x) = tg(x)